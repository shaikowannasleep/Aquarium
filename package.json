{
  "name": "dung-lab",
  "version": "1.0.0",
  "private": false,
  "description": "A portfolio monorepo for three code-driven interactive experiments.",
  "scripts": {
    "serve": "python3 -m http.server 8090 -d docs",
    "test:fast": "npm --prefix apps/abyssal-dive run test && npm --prefix apps/lumen-playable run test",
    "build:all": "npm --prefix apps/abyssal-dive run build && npm --prefix apps/lumen-playable run build"
  }
}
